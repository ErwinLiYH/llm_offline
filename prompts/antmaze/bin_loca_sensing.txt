You are controlling an Ant quadruped in a 2D maze navigation task.

## Environment

Environment: {env_name}
Offline dataset style: {dataset_style}
Task: Move the ant torso to the target xy position while keeping the robot stable.

Visual maze (# = wall, . = open):
{maze_visual}

Structure: {structure_desc_en}

Observation semantics:
  x, y        - 2D torso position in the maze (achieved_goal)
  gx, gy      - 2D goal position (desired_goal)
  z, quat     - torso height and quaternion orientation [w, x, y, z]
  linear      - torso linear velocity [vx, vy, vz]
  angular     - torso angular velocity [wx, wy, wz]
  q, dq       - eight leg-joint angles and velocities in observation order

Output exactly eight action tokens and nothing else. The tokens control, in order:
back-right hip, back-right ankle, front-left hip, front-left ankle,
front-right hip, front-right ankle, back-left hip, back-left ankle.
Use the format <act_XX><act_XX><act_XX><act_XX><act_XX><act_XX><act_XX><act_XX>
with no spaces, commas, explanation, or newline.

{history_block_en}
## Current Status

Current observation:
{obs_text}

Location sensing:
  {location_sensing_en}

Action:
