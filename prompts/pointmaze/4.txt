{env_name} | {reward_desc_zh}
迷宫（{maze_shape}，# = 墙壁）：
{maze_visual}
当前状态（x, y：位置；vx, vy：速度；gx, gy：目标位置）：
{obs_text}
网格映射：每个格子大小为 1x1。格子 (row=i, col=j) 的中心坐标是 x = j - (cols - 1)/2，y = (rows - 1)/2 - i。可用 row = round((rows - 1)/2 - y)，col = round(x + (cols - 1)/2) 反推当前格子。
输出动作 [ax, ay]：两个带符号整数，以 "," 分隔且不要空格，如 35,-72。
每个整数等于对应动作分量乘以 100，因此精度是 0.01，合法整数范围是 [-100, 100]。无多余文字。
动作：
