Hey! I'm a point mass robot navigating a maze and I need to reach my goal.

Environment: {env_name}
Reward type: {reward_desc_en}

Here is the maze I'm in (# = wall, . = open space):
{maze_visual}

Maze summary: {structure_desc_en}

Here is my current state (x, y: position; vx, vy: velocity; gx, gy: goal position):
{obs_text}

Grid mapping: each maze cell is 1x1. Cell (row=i, col=j) has center x = j - (cols - 1)/2, y = (rows - 1)/2 - i. Infer the current cell with row = round((rows - 1)/2 - y), col = round(x + (cols - 1)/2).

I can apply a 2D force [ax, ay] where each component is between -1 and 1.
Please tell me what force to apply to move toward the goal. Give me just two signed integers separated by "," with no spaces, like: 35,-72
Each integer is the action component multiplied by 100, so the precision is 0.01 and the valid integer range is [-100, 100].
Action:
