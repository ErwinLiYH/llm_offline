你正在控制一个二维迷宫中的质点机器人完成导航任务。

环境：{env_name}
任务：控制质点到达目标位置。
奖励类型：{reward_desc_zh}

迷宫布局（1=墙壁，0=可通行）：
原始矩阵：
{maze_raw_matrix}

可视化（# = 墙壁，. = 可通行）：
{maze_visual}

结构说明：{structure_desc_zh}

当前观测（6维状态向量）：
{obs_text}

观测语义：
  x, y   - 质点在迷宫中的二维位置
  vx, vy - 质点的二维速度
  gx, gy - 目标位置（desired_goal）

网格坐标映射：
  每个格子的大小是 1x1。
  对于迷宫矩阵中的格子 (row=i, col=j)，其中心坐标为 x = j - (cols - 1)/2，y = (rows - 1)/2 - i。
  若要根据观测中的 x,y 反推当前格子，可用 row = round((rows - 1)/2 - y)，col = round(x + (cols - 1)/2)。

动作空间（2维连续力向量）：
  ax - 水平力分量，范围 [-1, 1]
  ay - 垂直力分量，范围 [-1, 1]

请输出最优动作使质点向目标位置移动。
输出格式：两个带符号整数，以 "," 分隔且不要空格、不要小数点、不要括号、不要单位、不要说明文字。
每个整数表示对应动作分量乘以 100 后的结果，因此精度仍为 0.01。
每个整数的合法范围是 [-100, 100]。
示例：35,-72 表示 ax=0.35、ay=-0.72；-5,100 表示 ax=-0.05、ay=1.00
动作：
